# Generated by rig.ps1 and never committed: a throwaway SSH keypair for containers that exist for
# the length of one test run, a NuGet package that is a release artefact rather than source, and a
# copy of the collection staged into the build context because Docker cannot COPY from outside it.
.keys/
pkg/
collection/
