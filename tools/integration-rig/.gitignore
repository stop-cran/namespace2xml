# Generated by rig.ps1 and never committed: a throwaway SSH keypair for containers that exist for
# the length of one test run, and a NuGet package that is a release artefact rather than source.
.keys/
pkg/
