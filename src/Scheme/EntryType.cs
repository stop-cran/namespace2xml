namespace Namespace2Xml.Scheme
{
    public enum EntryType
    {
        root,
        key,
        type,
        filename,
        output,
        delimiter,
        substitute,
        xmloptions
    }
}
